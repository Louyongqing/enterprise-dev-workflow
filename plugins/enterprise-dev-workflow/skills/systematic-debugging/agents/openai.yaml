interface:
  display_name: "Systematic Debugging"
  short_description: "Find the root cause before making a focused fix"
  default_prompt: "Use $systematic-debugging to reproduce this failure, establish the root cause, and verify the smallest fix."
policy:
  allow_implicit_invocation: false
