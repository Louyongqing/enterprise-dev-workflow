interface:
  display_name: "Requesting Code Review"
  short_description: "Run risk-scaled independent code review"
  default_prompt: "Use $requesting-code-review to review this implementation against its approved requirements and risk."
policy:
  allow_implicit_invocation: false
